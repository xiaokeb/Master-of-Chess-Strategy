# 中国象棋存档终局前缀验收

日期：2026-09-25。原生目标使用指定 Android NDK 30 LLVM 编译，x86_64 测试二进制在 Pixel_7_Pro AVD 执行。

构造两轮士往返后第三次出现相同局面，规则判和；伪造第九着红仕合法走位，更新棋盘、计数、声明的结果与 CRC。修复前恢复器错误接受，新增原生断言先失败；修复后拒绝为损坏数据。另将普通兵前进的历史性质伪造成“将军”并更新 CRC，恢复后性质重新变为“闲”。

另一反例从已达到自然限着并判和的初始状态伪造车吃马，虽局面与 CRC 一致，恢复仍拒绝。x86_64 原生规则可执行文件在 AVD 退出码为 0。

完整 Gradle 门禁：JVM 202/202、App AVD 50/50、JNI AVD 4/4、Debug APK 构建通过，Lint 0 错误、3 条依赖版本提醒。仍不宣称职业棋例已穷尽。
