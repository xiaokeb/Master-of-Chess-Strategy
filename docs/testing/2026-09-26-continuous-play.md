# 中国象棋连续完整对局验收

日期：2026-09-26。环境：Pixel_7_Pro Android 17 x86_64 AVD、Debug APK、正式 MainActivity/导航、
Room、Native 规则、Pikafish 与随包 NNUE。仅新增验收工具，未修改正式逻辑、搜索参数或许可文件。

## 配置与结果

简单档双 AI、标准初始局面、4 倍观看速度、不限棋钟、自动续局开启、上限 3 局、音效开启。
测试不调用认输，不注入终局，不使用替代引擎；所有结果由当前原生规则自然裁定。

最终 runId：`91aee607626a441d9d82c2704315801a`，应用 PID：22919。
设备报告耗时 191376 ms，JUnit 耗时 193.614 秒，`OK (1 test)`；主机脚本退出码 0。
原始报告、完整棋谱、采样、日志及截图保留于本地 `.build/continuous-play/<runId>/`。

| 棋局 ID | 单步着数 | 结果 |
| --- | ---: | --- |
| 2696ec1f-b3eb-4bec-87d5-844d1751c0a6 | 112 | 黑胜 |
| 549a9e03-7614-4b9d-a001-9e81d67865de | 78 | 黑胜 |
| d0a12de7-15ad-4e5a-b21b-acd55b84d532 | 112 | 和棋 |

共 302 步。每份棋谱由独立原生规则实例恢复全部合法历史、重算结果，并逐步撤回至字节一致的
标准初始局面；不存在提前认输/超时拼造的“完整对局”。这仍不替代规则棋例或棋力梯度验收。

## 已覆盖

- 点击正式暂停按钮，待搜索/存档收尾后静置 3 秒，整份存档不变，服务和唤醒锁释放；再点恢复继续落子。
- 实际熄屏 90 秒，跨越 30 秒检查和 60 秒无进展预算窗口；持续有进展，共新增 48 步。
  后台样本显示服务/唤醒锁均开启，返回后仍为相同 ViewModel，唤醒锁释放。
- 三份独立棋谱与自然终局结果一致，最后活动存档与第三份棋谱字节一致；排位账本为空。
- 第三局后自动暂停，服务/唤醒锁停止，静置 5 秒不创建第四局、不重复结算。
- 正式返回导航释放租约；已退出的 ViewModel 无法重新注册运行时，调用重开不再修改存档。
- 实际界面含正式音效组件；本测试不对人耳听感或硬件扬声器输出作通过声明。

## 资源观测与限制

共 14 个样本，通常间隔约 15 秒，另含阶段边界。没有主动 GC、改变生产搜索预算或用任意内存阈值判绿。

| 指标 | 本轮范围/结果 |
| --- | --- |
| PSS | 308248–319158 KiB |
| 原生已分配内存 | 158378704–160589440 字节 |
| 线程数 | 38–44 |
| 文件描述符数 | 140–177 |
| 到达续局上限及退出 | 服务 false、唤醒锁 false |

这些是包含测试组件的 Debug/AVD 样本；终局和退出后进程还持有其他页面、框架及引擎缓存，
不要求所有内存返回初始值，也不能由三局采样宣称“无泄漏”。尚未完成小时级挂机、Release 帧耗时、
真机耗电、实体设备大师级单步 ≤2 秒及正式音效试听验收。

截图 `game.png` 确认实际棋盘与演局控件，但浅色页面上的白色系统状态栏可读性不足，列入下一界面修正节点。

## 失败保留与修正

首次编译误用不存在的 `GameResult.code`，改为项目正式棋谱解码后构建通过。
首次运行 `adb6389830bf4a53a49cf7f8884784f1` 完成 163/137/111 步三局，但最终断言失败，
整项不计通过：测试错误地把退出页面保留的 `uiState.isEngineAvailable` 当作原生资源是否关闭。
正常 `onCleared` 关闭引擎而不重写旧 UI；改为检查以实际引擎所有权为条件的运行时注册，并验证
退出对象不能再重开/写入，随后重跑整个专项通过。没有删除资源释放要求或修改正式逻辑迁就测试。

## 复现与门禁

```powershell
.\gradlew.bat :app:assembleDebug :app:assembleDebugAndroidTest --console=plain
.\tools\android\test-continuous-play.ps1 -AdbPath 'E:\Backend_Env\SDK\platform-tools\adb.exe' -DeviceSerial emulator-5554 -AllowEmulatorDataReset
```

仅允许指定模拟器，脚本清空本项目测试数据且不可恢复；禁止与其他设备测试并行。
两 APK 构建和法律副本/权重/音效/内容门禁通过；Lint 0 错误、3 条既有依赖提醒，PowerShell 语法解析无错误。
没有正式代码变更，因此未重复未改动的 235 项 App JVM、C++/JNI、迁移或全部历史设备用例。
