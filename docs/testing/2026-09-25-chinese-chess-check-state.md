# 中国象棋将军提示验收

范围：新增 `ChineseChessRuleEngine.isInCheck(side)` 的 JNI 只读查询，ViewModel 将当前行棋方的受将军状态写入不可变快照；横屏状态文案与非盲棋将帅警示圈消费该状态。盲棋不显示光圈。

验证：NDK 30 LLVM 双 64 位 ABI 构建；JVM 全模块回归；Pixel_7_Pro AVD 上 App 51/51、JNI 5/5。JNI 测试覆盖双方初始无将、红帅被将、应将后清除及悔棋恢复。残局仪器测试额外拒绝直接吃将，并要求唯一获胜首着后黑方确实被将军。横屏 Compose 画面已截取并检查布局与警示圈。

2026-09-26 收尾复核：JVM XML 合计 205/205；保留的界面用例 8/8、残局用例 2/2 分别定向运行通过。唯一解统计包括所有获胜着，另断言获胜时对方被将军，避免忽略另一条困毙胜着。临时截图写入代码已移除，APK 和 Lint 门禁通过（3 条版本提醒）。

局限：模拟器视觉测试不能替代实体设备上的触控、色彩对比度和音效试听；Pikafish/NNUE 的 GPL 与独立非商业边界没有变化。
