# 2026-09-26 AI 先手与玩家身份验收

## 环境与范围

Windows、JDK 17、Android SDK / NDK 30.0.16138531，Pixel_7_Pro Android 17 x86_64 AVD。
直接在现有项目开发，无 worktree、MinGW、新依赖或第三方资产。正式 Native 规则、
Pikafish 与固定 NNUE 由现有构建提供；本轮只改 Kotlin/UI/Room 及相应文档。

## 已通过

| 检查 | 结果 | 证据范围 |
| --- | --- | --- |
| App JVM | 203/203，37 个测试类 | `:app:testDebugUnitTest`；本次为 App 模块全量，不含其他模块 |
| AI 先手状态机 | 7/7（包含在上行） | 开局权限、保留开局着的悔棋、提示/提和、断点恢复、执黑胜负音效/统计/棋谱、双方超时、四类挑战 |
| 备份编解码 | 13/13（包含在 App JVM） | v3 身份往返、v1/v2 老格式红方默认、旧设置保留及无效身份拒绝 |
| 视图变换 | 6/6（包含在 App JVM） | 正反视角、不同宽高比、缩放与平移后落点映射及反馈边界 |
| 新完整导航 | 1/1 | 设置启用 → 真实 Pikafish 红方开局 → 玩家执黑合法走子 → 悔棋 → 改偏好 → 退出重进不换方 → 重开应用新偏好 |
| Room 迁移 | 12/12 | 11→12 保留棋谱结果/收藏/引擎字节/着数，身份默认红方；1→12 全链校验通过 |
| Room 备份恢复 | 2/2 | 七类数据原子替换，AI 先手设置与执黑会话/棋谱往返；损坏备份不改当前数据 |
| 既有导航 | 22/22 | 设置、许可、棋谱及既有模式入口 |
| 共享棋盘交互 | 5/5 | 双指手势、点击、只读与盲棋边界 |
| 教程练习导航 | 1/1 | 横屏缩放后 Native 通关、首通奖励与返回教程 |
| Lint | 0 错误、3 条既有依赖版本提醒 | 新增参数顺序提醒已修正 |
| Debug APK / 资产门禁 | 通过 | 许可副本、固定 NNUE、原创音效、发布内容来源与哈希检查 |

设备专项合计 43 项，不与 App JVM 重复计数。每个设备测试类单独执行，并查看
JUnit XML 的类名/数量；不使用逗号拼接过滤器推断多类均已执行。

首轮新导航测试因横屏首页“设置”按钮位于资料卡滚动区之外而未进入设置页；
修正为先滚动到按钮再点击后通过。对应生产滚动功能未改动，也未用重试掩盖失败。

## 演示

- 实际模拟器截图：工作区 `.build/ai-first.png`（本地测试产物，不进入发布资产）。
- Android Studio：`PreviewCatalog.kt` → “18 AI 先手与玩家执黑”，为 Debug 专用静态预览。
- 黑方在棋盘下方、棋字正向，标题明确双方身份；实际走子由原生引擎校验。

## 未验收项

本轮不代表象棋全部完成。自然限着数字化口径仍待选择，相邻难度梯度、复杂棋例、
让子与后台熄屏边界、内容规模仍有后续工作。真机触控、音效试听和性能未用模拟器
替代；没有宣称取得 NNUE 商业授权，继续执行现有发布限制。
